spring:
  application:
    name: approval
management:
  context_path: /admin
security:
  user:
    password: password
  oauth2:
    resource:
      token-info-uri: /openid-connect-server-webapp/token
